# Admin App

Minimal Vite + React scaffold for the Admin frontend.

Quick start:

```powershell
cd frontend/admin_app
npm install
npm run dev
```

This is a tiny scaffold — replace `src/App.jsx` with your real application.
